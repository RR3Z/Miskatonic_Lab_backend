# API error catalog

The machine-readable API error contract is maintained in
`pkg/errors/error_catalog.json`.

User-facing Russian explanations and troubleshooting live in the frontend
catalog at `Miskatonic_Lab_frontend/src/data/errors/error-catalog.ru.json` and
are published by the `/errors/[code]` route.
