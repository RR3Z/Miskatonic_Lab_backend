// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: getNote.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getNote = `-- name: GetNote :one
SELECT n.id, n.character_id, n.title, n.body, n.created_at, n.updated_at
FROM notes n
JOIN characters c ON c.id = n.character_id
WHERE c.user_id = $1
  AND n.character_id = $2
  AND n.id = $3
`

type GetNoteParams struct {
	UserID      string      `json:"user_id"`
	CharacterID pgtype.UUID `json:"character_id"`
	NoteID      pgtype.UUID `json:"note_id"`
}

func (q *Queries) GetNote(ctx context.Context, arg GetNoteParams) (Note, error) {
	row := q.db.QueryRow(ctx, getNote, arg.UserID, arg.CharacterID, arg.NoteID)
	var i Note
	err := row.Scan(
		&i.ID,
		&i.CharacterID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
